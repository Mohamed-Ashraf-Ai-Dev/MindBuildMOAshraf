# Project-specific R8 rules can be added by the AIDE-like editor here.
# Keep model classes used by the build integration if the host app enables shrinking.
-keep class com.mindbuildmoashraf.engine.** { *; }
